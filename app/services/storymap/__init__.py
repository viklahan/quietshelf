"""Story Map: read a short manuscript and reflect its characters and their
relationships back as structured JSON for a visual corkboard. A mirror - it
reflects what the text contains; it never authors, prescribes, or invents.

Independent of the other services: shared code is reached only through
app.ingest (file reading) and app.providers (the LLM + JSON layer).
"""
