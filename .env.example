# Which free AI backend to use: gemini | groq | ollama | openrouter
# All are free - there is no paid provider in this codebase.
# (The Format service uses no AI at all.)
LLM_PROVIDER=gemini

# Required when LLM_PROVIDER=gemini. Free key, no card:
#   https://aistudio.google.com
GEMINI_API_KEY=

# Required when LLM_PROVIDER=groq. Free key, no card:
#   https://console.groq.com
GROQ_API_KEY=

# Required when LLM_PROVIDER=openrouter. Free key, no card:
#   https://openrouter.ai/keys
# Paste ONLY the key (sk-or-...). Do NOT add an inline "# comment" after it -
# the .env loader keeps the comment as part of the value and the key breaks.
OPENROUTER_API_KEY=

# Optional. When the configured model 404s ("unavailable for free"), the app
# walks this comma-separated list and uses the first one that works, so it
# self-heals when a free slug is retired. All must support JSON output.
# Leave unset to use the built-in list. Find current free models at
#   https://openrouter.ai/models?max_price=0
OPENROUTER_FALLBACK_MODELS=

# Used when LLM_PROVIDER=ollama. No key needed - fully local/offline.
# Install free at https://ollama.com, then: ollama pull qwen2.5:latest
# Running the app in Docker with Ollama on the host? Use:
#   OLLAMA_HOST=http://host.docker.internal:11434
OLLAMA_HOST=http://localhost:11434

# Optional model override (otherwise a sensible default per provider):
#   gemini: gemini-2.5-flash | groq: llama-3.3-70b-versatile
#   ollama: qwen2.5:latest | openrouter: openai/gpt-oss-20b:free
# Note: OpenRouter's free model slugs change over time. If you get a 404 that
# says a model is "unavailable for free", pick another ":free" model from
# https://openrouter.ai/models?max_price=0
MODEL_NAME=

# Optional shared access code; if set, requests need a matching X-Access-Code
# header. Leave unset to keep the API open (fine for self-hosters).
ACCESS_CODE=

# Optional. Comma-separated CORS origins. Default: * (open).
ALLOWED_ORIGINS=*

# Optional. Requests per hour per IP - protects your free-tier quota.
# Read as an integer; the "/hour" suffix is documentation only.
RATE_LIMIT=20/hour

# Optional. Client-side pacing: max requests per minute the app sends to the
# provider, so bursts (Promote maps chunks in parallel) queue instead of
# hitting free-tier 429s. Defaults: gemini 8 (free tier allows ~10), others
# unpaced. 0 disables. Same pattern for GROQ_RPM / OPENROUTER_RPM.
GEMINI_RPM=8

# Optional. Max upload size in MB for manuscript files.
MAX_UPLOAD_MB=25
